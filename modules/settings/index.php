<?php require_once __DIR__.'/../../config/app.php'; requireAuth(); $pageTitle='Settings';
$msg=''; if($_SERVER['REQUEST_METHOD']==='POST'){verifyCsrf(); $curr=post('current_password'); $new=post('new_password'); $u=fetchOne('SELECT * FROM users WHERE id=?',[$_SESSION['user']['id']]); if($u && password_verify($curr,$u['password_hash'])){executeSql('UPDATE users SET password_hash=? WHERE id=?',[password_hash($new,PASSWORD_DEFAULT),$u['id']]); $msg='Password changed.';} else {$msg='Current password invalid.';}}
include __DIR__.'/../../includes/header.php'; include __DIR__.'/../../includes/sidebar.php'; ?><main class="content-wrap"><?php include __DIR__.'/../../includes/topbar.php'; ?><section class="panel glass" style="max-width:600px"><h5>Change Password</h5><?php if($msg):?><div class="alert alert-info"><?=e($msg)?></div><?php endif;?><form method="post" class="vstack gap-2"><?=csrfField()?><input type="password" name="current_password" class="form-control" placeholder="Current Password" required><input type="password" name="new_password" class="form-control" placeholder="New Password" required><button class="btn btn-primary">Update</button></form></section></main><?php include __DIR__.'/../../includes/footer.php'; ?>
